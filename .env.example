# Copy to .env.local (Vite loads .env.local automatically)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Feature flags
# Enable the Smart Notifications Drawer UI (1 to enable)
VITE_NOTIFICATIONS_V1=0

# Server-side (Vercel) only
# SUPABASE_SERVICE_ROLE_KEY is required by /api/cron-generate-notifications to call the Edge Function
# Do NOT expose this in the browser; set it in Vercel Project Settings → Environment Variables
# SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY
